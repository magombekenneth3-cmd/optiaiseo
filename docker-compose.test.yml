version: '3.8'

services:
  test-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: aiseo_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
    # No volume — disposable, fast
